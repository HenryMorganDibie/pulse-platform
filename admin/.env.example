# The deployed (or local) pulse-platform API URL. No trailing slash.
# The admin secret itself is NOT an env var here — it's entered at runtime
# on /login and kept in sessionStorage only. See lib/api.ts.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
