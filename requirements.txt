# Pulse Platform — dependencies

# API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# DB (Neon/Supabase Postgres + pgvector)
asyncpg>=0.29.0
pgvector>=0.2.5

# LLM (Groq, Ollama) and embeddings (HuggingFace Inference API) — all plain
# HTTP calls via httpx, no local model weights. See src/products/recommend/
# embeddings.py for why: sentence-transformers/torch measured at 1.1GB
# installed, far past Vercel's serverless function bundle limit.
httpx>=0.27.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
